# Sprint Snapshots

This directory holds committed sprint render artifacts when a shared view is intentionally needed.

The live source of truth is the repo-local `.sprintctl/sprintctl.db`, which is gitignored.
