[pytest]
addopts = -ra
testpaths = tests
markers =
    unit: Focused unit-level behavior.
    docs: Documentation and repository contract checks.
    integration: Optional cross-repository integration checks.
